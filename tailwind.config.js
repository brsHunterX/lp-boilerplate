/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{html,js}'],
  theme: {
    extend: {
      // This configuration was used as an example.
      // When configuring your LP remove the content below.
      colors: {
        primary: '#328AEA',
      },
    },
  },
  plugins: [],
};
